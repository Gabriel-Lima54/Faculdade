#include <stdio.h>
main (){
	int num, primos = 0, i, quant, aux = 100
	printf("Digite o valor de numeros primos: ");
	scanf("%d", &num);
	printf("%d numeros primos depois de 100", num);
	while (primos > num){
		aux++;
		cont=0
	}
	
}
	
